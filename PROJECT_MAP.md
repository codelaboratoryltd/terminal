# Project Map

Last updated: 2026-06-09

This file is the LLM navigation guide. Read it before exploring code. Update it when you
add, remove, or significantly change files or packages.

---

## Package: `github.com/fyne-io/terminal` (root)

The terminal emulator library. All files sit in the repo root.

| File | Lines | What it does / When to look here |
|------|-------|----------------------------------|
| `term.go` | 1471 | **Start here for almost everything.** `Terminal` struct, widget lifecycle (`CreateRenderer`, `Resize`, `Show`), PTY read loop, `Config`/`Listener` pattern, scroll buffer, resize signalling. |
| `escape.go` | 943 | ANSI escape sequence parser. Handles CSI (`\x1b[…`), OSC (`\x1b]…`), DCS, APC, private modes, and the `handleEscape` dispatch table. Touch here for any VT/xterm sequence bug. |
| `output.go` | 579 | Executes parsed output: writes characters to the cell grid, handles special chars (`\n`, `\r`, `\t`, `\b`), charset mapping (G0/G1), line wrap, insert/delete mode. Touch here for character rendering or cursor movement bugs. |
| `render.go` | 587 | Rendering pipeline: `cellSize()`, font metrics, cursor cell painting, handing the cell grid to `TermGrid`, scroll region repaint. Touch here for layout, font sizing, or cursor visibility issues. |
| `color.go` | 424 | ANSI 16-color + 256-color palette → `color.NRGBA`. Theme-aware foreground/background. Touch here for any color mapping bug. |
| `input.go` | 305 | Keyboard events → PTY byte sequences. Key-to-sequence table for function keys, modifiers, numpad. Touch here for keyboard input bugs. |
| `input_other.go` | 11 | Non-Windows keyboard shim (empty/minimal). |
| `input_windows.go` | 45 | Windows-specific key handling differences. |
| `mouse.go` | 76 | Mouse events → PTY escape sequences (button, scroll, SGR mode). Touch here for mouse reporting bugs. |
| `select.go` | 121 | Text selection (click-drag, double-click word, triple-click line). Stores selection range, exports selected text. |
| `osc.go` | 146 | OSC command handlers: title (`OSC 0/2`), working dir (`OSC 7`), fg/bg/cursor colour set+query (`OSC 10/11/12`), shell-integration marks (`OSC 133`). `OSC 8`/`OSC 52` only work if a handler is registered via `RegisterOSCHandler` — no built-in default. |
| `apc.go` | 72 | APC and DCS command handlers (less common — Kitty graphics protocol stubs live here). |
| `position.go` | 52 | `position{row, col}` type, `cellForPos()` pixel→cell conversion, bounds helpers. |
| `term_unix.go` | 50 | Unix PTY open (`creack/pty`), SIGWINCH signal forwarding on resize. |
| `term_windows.go` | 57 | Windows PTY via `ActiveState/conpty`. |

### Test Files (root)

| File | Tests for |
|------|-----------|
| `term_test.go` | Widget lifecycle, config, basic I/O |
| `escape_test.go` | ANSI sequence parsing correctness (largest test file) |
| `output_test.go` | Character output, cursor movement |
| `color_test.go` | Color mapping (ANSI 16 + 256 palette) |
| `input_test.go` | Keyboard → byte sequence mapping |
| `mouse_test.go` | Mouse event encoding |
| `select_test.go` | Selection range logic |
| `osc_test.go` | OSC command handling |
| `apc_test.go` | APC/DCS handling |
| `printing_test.go` | Printing/screen-dump functionality |

---

## Package: `github.com/fyne-io/terminal/internal/widget`

Custom Fyne rendering widgets. Performance-critical — do not add per-cell objects here.

| File | Lines | What it does / When to look here |
|------|-------|----------------------------------|
| `termgrid.go` | 375 | **`TermGrid` widget** — the main display surface. Owns the cell matrix (`[][]TermTextGridCell`), dirty-region tracking, blink cursor timer, `Refresh()` gating. Touch here for refresh/repaint or dirty-render issues. |
| `termgridhelper.go` | 339 | Styling helpers: `TermTextGridStyle` (fg/bg colors, bold/italic/underline), highlight-range management, blink mode toggle, `SetCell`/`GetCell` helpers. Touch here for cell styling or highlight bugs. |
| `termraster.go` | 384 | **Single-texture raster renderer.** Draws all cells into one `canvas.Raster` using `golang.org/x/image` glyph blitting. Contains the global font cache (`rasterFontData`). Touch here for rendering artefacts, glyph placement, or font-load issues. |
| `termgridhelper_test.go` | 131 | Tests for style helpers and highlight ranges. |

---

## Package: `github.com/fyne-io/terminal/cmd/fyneterm`

Standalone terminal application (the user-facing app).

| File | Lines | What it does / When to look here |
|------|-------|----------------------------------|
| `main.go` | 158 | App entry point. Creates `fyne.App`, window, `terminal.Terminal` widget, wires config listener, handles `--version` flag. |
| `theme.go` | 140 | Custom Fyne theme: ANSI color overrides, monospace font injection, font size preference. |
| `data/bundled.go` | 17 | Embedded app icon resource (generated by `fyne bundle`). |
| `data/icons.go` | 10 | Icon constant definitions. |
| `font/NotoSansMono/notosansmono.go` | 26 | Embeds NotoSansMono TTF into the binary. |
| `translation/` | — | i18n `.toml` files — touch for adding/changing UI strings. |

---

## Package: `github.com/fyne-io/terminal/cmd/fyneterm-old` (archive)

Legacy version of the app, kept for reference. Uses DejaVu font. Do not add features here.

---

## Key Types Quick-Reference

| Type | File | Purpose |
|------|------|---------|
| `Terminal` | `term.go` | Root widget — everything hangs off this |
| `Config` | `term.go` | Rows, Cols, Title; passed to `Listener` on change |
| `Listener` | `term.go` | Interface: `OnClosed()`, `OnTitleChanged()` |
| `TermGrid` | `internal/widget/termgrid.go` | The Fyne widget that displays the cell matrix |
| `TermTextGridStyle` | `internal/widget/termgridhelper.go` | Per-cell fg/bg/attrs |
| `rasterFontData` | `internal/widget/termraster.go` | Cached font face + metrics |
| `position` | `position.go` | `{row, col}` within the terminal grid |

---

## Where to Look for Common Tasks

| Task | File(s) |
|------|---------|
| Terminal sequence not handled | `escape.go` → dispatch table |
| Character appears wrong / missing | `output.go` → `handleOutput()` |
| Colors wrong | `color.go` → `toColor()` |
| Keyboard shortcut not working | `input.go` → key map table |
| Mouse clicks not sent | `mouse.go` |
| Window title not updating | `osc.go` → OSC 0/1/2 handlers |
| Rendering artefact / wrong glyph | `internal/widget/termraster.go` |
| Dirty render / repaint issue | `internal/widget/termgrid.go` |
| Cell style (bold, underline, color) | `internal/widget/termgridhelper.go` |
| App window / theming | `cmd/fyneterm/main.go`, `cmd/fyneterm/theme.go` |
| Resize / font size | `render.go` → `cellSize()` |
| PTY won't open (Unix) | `term_unix.go` |
| PTY won't open (Windows) | `term_windows.go` |

---

## External Dependencies (key ones)

| Dep | Why it's here |
|-----|---------------|
| `fyne.io/fyne/v2` (local fork) | GUI framework — **local override at `drunlade/fyne`** |
| `github.com/creack/pty` | Unix PTY open/resize |
| `github.com/ActiveState/termtest/conpty` | Windows ConPTY |
| `golang.org/x/image` | Low-level glyph drawing in the raster |
| `github.com/go-text/typesetting` | Font shaping |
| `github.com/go-text/render` | Text rendering pipeline |
